# ADR-001 Arquitectura por capas

Estado: aceptada.

HoralyApp separa reglas puras (`domain`), casos de uso (`application`), persistencia/adapters (`infrastructure`), UI (`components`) y plugins (`lib/plugins`). La UI no debe depender directamente de Supabase.
