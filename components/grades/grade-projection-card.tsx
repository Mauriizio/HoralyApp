import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import type { SubjectProjection } from "@/domain/grading"
export function GradeProjectionCard({ projection }: { projection: SubjectProjection }) {
  return <Card><CardHeader><CardTitle>Resultados y proyecciones</CardTitle><CardDescription>Resultados reales, proyección y simulación se muestran por separado.</CardDescription></CardHeader><CardContent className="grid gap-2 text-sm sm:grid-cols-2"><p>Promedio de presentación: {projection.presentationAverage ?? "Sin datos"}</p><p>Promedio evaluado: {projection.evaluatedAverage ?? "Sin datos"}</p><p>Contribución acumulada: {projection.currentContribution}</p><p>Porcentaje ya evaluado: {projection.confidence}</p><p>Proyección: {projection.projectedFinalGrade ?? "Estructura inválida"}</p><p>Nota definitiva: {projection.definitiveFinalGrade ?? "Pendiente"}</p><p>Transversal necesario: {projection.requiredFinalExamScore?.requiredScore ?? "No aplica"}</p><p>Mínimo final posible: {projection.minimumPossibleFinalGrade}</p><p>Máximo final posible: {projection.maximumPossibleFinalGrade}</p><p>Estado: {projection.status}</p></CardContent></Card>
}
