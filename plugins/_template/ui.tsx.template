"use client"

import type { ToolPluginProps } from "@/lib/plugins/plugin-types"
import { examplePureFunction } from "./domain"

export default function __PLUGIN_COMPONENT__({ locale, theme }: ToolPluginProps) {
  return (
    <section className="space-y-3" aria-label="__PLUGIN_NAME__">
      <h3 className="font-semibold">__PLUGIN_NAME__</h3>
      <p className="text-sm text-muted-foreground">Locale: {locale}. Tema: {theme}.</p>
      <p>{examplePureFunction("Implementa tu herramienta aquí")}</p>
    </section>
  )
}
